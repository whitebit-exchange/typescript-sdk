// This file was auto-generated by Fern from our API Definition.

import * as WhitebitApi from "./api/index.js";
import { CodesClient } from "./api/resources/codes/client/Client.js";
import { CollateralTradingClient } from "./api/resources/collateralTrading/client/Client.js";
import { CreditLineClient } from "./api/resources/creditLine/client/Client.js";
import { DepositClient } from "./api/resources/deposit/client/Client.js";
import { FeesClient } from "./api/resources/fees/client/Client.js";
import { JwtClient } from "./api/resources/jwt/client/Client.js";
import { MainAccountClient } from "./api/resources/mainAccount/client/Client.js";
import { MarketFeeClient } from "./api/resources/marketFee/client/Client.js";
import { PublicApiV4Client } from "./api/resources/publicApiV4/client/Client.js";
import { SpotTradingClient } from "./api/resources/spotTrading/client/Client.js";
import { SubAccountClient } from "./api/resources/subAccount/client/Client.js";
import { SubAccountApiKeysClient } from "./api/resources/subAccountApiKeys/client/Client.js";
import { TransferClient } from "./api/resources/transfer/client/Client.js";
import { TravelRuleClient } from "./api/resources/travelRule/client/Client.js";
import { WithdrawClient } from "./api/resources/withdraw/client/Client.js";
import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "./BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "./core/headers.js";
import * as core from "./core/index.js";
import * as environments from "./environments.js";
import { handleNonStatusCodeError } from "./errors/handleNonStatusCodeError.js";
import * as errors from "./errors/index.js";

export declare namespace WhitebitApiClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class WhitebitApiClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<WhitebitApiClient.Options>;
    protected _publicApiV4: PublicApiV4Client | undefined;
    protected _mainAccount: MainAccountClient | undefined;
    protected _deposit: DepositClient | undefined;
    protected _jwt: JwtClient | undefined;
    protected _withdraw: WithdrawClient | undefined;
    protected _transfer: TransferClient | undefined;
    protected _codes: CodesClient | undefined;
    protected _fees: FeesClient | undefined;
    protected _subAccount: SubAccountClient | undefined;
    protected _subAccountApiKeys: SubAccountApiKeysClient | undefined;
    protected _creditLine: CreditLineClient | undefined;
    protected _travelRule: TravelRuleClient | undefined;
    protected _collateralTrading: CollateralTradingClient | undefined;
    protected _marketFee: MarketFeeClient | undefined;
    protected _spotTrading: SpotTradingClient | undefined;

    constructor(options: WhitebitApiClient.Options) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    public get publicApiV4(): PublicApiV4Client {
        return (this._publicApiV4 ??= new PublicApiV4Client(this._options));
    }

    public get mainAccount(): MainAccountClient {
        return (this._mainAccount ??= new MainAccountClient(this._options));
    }

    public get deposit(): DepositClient {
        return (this._deposit ??= new DepositClient(this._options));
    }

    public get jwt(): JwtClient {
        return (this._jwt ??= new JwtClient(this._options));
    }

    public get withdraw(): WithdrawClient {
        return (this._withdraw ??= new WithdrawClient(this._options));
    }

    public get transfer(): TransferClient {
        return (this._transfer ??= new TransferClient(this._options));
    }

    public get codes(): CodesClient {
        return (this._codes ??= new CodesClient(this._options));
    }

    public get fees(): FeesClient {
        return (this._fees ??= new FeesClient(this._options));
    }

    public get subAccount(): SubAccountClient {
        return (this._subAccount ??= new SubAccountClient(this._options));
    }

    public get subAccountApiKeys(): SubAccountApiKeysClient {
        return (this._subAccountApiKeys ??= new SubAccountApiKeysClient(this._options));
    }

    public get creditLine(): CreditLineClient {
        return (this._creditLine ??= new CreditLineClient(this._options));
    }

    public get travelRule(): TravelRuleClient {
        return (this._travelRule ??= new TravelRuleClient(this._options));
    }

    public get collateralTrading(): CollateralTradingClient {
        return (this._collateralTrading ??= new CollateralTradingClient(this._options));
    }

    public get marketFee(): MarketFeeClient {
        return (this._marketFee ??= new MarketFeeClient(this._options));
    }

    public get spotTrading(): SpotTradingClient {
        return (this._spotTrading ??= new SpotTradingClient(this._options));
    }

    /**
     * The endpoint creates a quote for converting one currency to another. Quote lifetime is 10 seconds, then quote will be expired.
     *
     * The minimum convert size is derived from the resulting proceeds, not a fixed per-pair floor: a request is rejected as too small when `rate × amount` rounds to zero in the target currency. Account balance is pre-checked at estimate time and re-checked at confirm time, because balance can change within the 10-second quote window. There is an absolute server-side maximum on the conversion amount.
     *
     * <Note>
     * The endpoint can be used to obtain a pre-execution price estimate for a market order. Call the endpoint with the desired amount before placing a market order to see the approximate execution price.
     * </Note>
     *
     * <Note>
     * Error `message` values may be returned as translation keys (for example `validation.required`) rather than finalized English strings. Treat the `code` and the field name under `errors` as the stable contract.
     * </Note>
     *
     * <Warning>
     * Rate limit: 10000 requests/10 sec.
     * </Warning>
     *
     * @param {WhitebitApi.ConvertEstimateRequest} request
     * @param {WhitebitApiClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link WhitebitApi.BadRequestError}
     * @throws {@link WhitebitApi.UnprocessableEntityError}
     *
     * @example
     *     await client.convertEstimate({
     *         from: "BTC",
     *         to: "USDT",
     *         direction: "to",
     *         amount: "35,103.1"
     *     })
     *
     * @example
     *     await client.convertEstimate({
     *         from: "BTC",
     *         to: "USDT",
     *         direction: "from",
     *         amount: "1"
     *     })
     */
    public convertEstimate(
        request: WhitebitApi.ConvertEstimateRequest,
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): core.HttpResponsePromise<WhitebitApi.ConvertEstimateResponse> {
        return core.HttpResponsePromise.fromPromise(this.__convertEstimate(request, requestOptions));
    }

    private async __convertEstimate(
        request: WhitebitApi.ConvertEstimateRequest,
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): Promise<core.WithRawResponse<WhitebitApi.ConvertEstimateResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-TXC-PAYLOAD": requestOptions?.txcPayload ?? this._options?.txcPayload,
                "X-TXC-SIGNATURE": requestOptions?.txcSignature ?? this._options?.txcSignature,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (
                        (await core.Supplier.get(this._options.environment)) ??
                        environments.WhitebitApiEnvironment.Default
                    ).base,
                "api/v4/convert/estimate",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as WhitebitApi.ConvertEstimateResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new WhitebitApi.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new WhitebitApi.UnprocessableEntityError(
                        _response.error.body as unknown,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WhitebitApiError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/v4/convert/estimate");
    }

    /**
     * The endpoint confirms an estimated quote.
     *
     * An expired quote returns code `20` (`api.converter.quoteExpired`), which is distinct from a quote that could not be found (code `0`). Balance is re-checked at confirm time and can fail with code `37` even when the estimate succeeded, because balance may change within the 10-second quote window.
     *
     * <Note>
     * A re-confirmed (already used) quote and a quote that never existed both return code `0` with the `quoteId` field key `frontendServerSide.converter.quoteInvalid`. The response alone does not distinguish "already used" from "never existed".
     * </Note>
     *
     * <Note>
     * Error `message` values may be returned as translation keys (for example `api.converter.quoteExpired`) rather than finalized English strings. Treat the `code` and the field name under `errors` as the stable contract.
     * </Note>
     *
     * <Warning>
     * Rate limit: 10000 requests/10 sec.
     * </Warning>
     *
     * @param {WhitebitApi.ConvertConfirmRequest} request
     * @param {WhitebitApiClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link WhitebitApi.BadRequestError}
     * @throws {@link WhitebitApi.UnprocessableEntityError}
     *
     * @example
     *     await client.convertConfirm({
     *         quoteId: "4050"
     *     })
     */
    public convertConfirm(
        request: WhitebitApi.ConvertConfirmRequest,
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): core.HttpResponsePromise<WhitebitApi.ConvertConfirmResponse> {
        return core.HttpResponsePromise.fromPromise(this.__convertConfirm(request, requestOptions));
    }

    private async __convertConfirm(
        request: WhitebitApi.ConvertConfirmRequest,
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): Promise<core.WithRawResponse<WhitebitApi.ConvertConfirmResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-TXC-PAYLOAD": requestOptions?.txcPayload ?? this._options?.txcPayload,
                "X-TXC-SIGNATURE": requestOptions?.txcSignature ?? this._options?.txcSignature,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (
                        (await core.Supplier.get(this._options.environment)) ??
                        environments.WhitebitApiEnvironment.Default
                    ).base,
                "api/v4/convert/confirm",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as WhitebitApi.ConvertConfirmResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new WhitebitApi.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new WhitebitApi.UnprocessableEntityError(
                        _response.error.body as unknown,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WhitebitApiError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/v4/convert/confirm");
    }

    /**
     * The endpoint returns convert history, sorted by `id` descending (newest first).
     *
     * The `from`–`to` window is capped at 30 days per request, even though data is retained for 6 months. A wider range is rejected with code `30` (`api.validation.dateTime.maxRange`).
     *
     * <Warning>
     * Rate limit: 10000 requests/10 sec.
     * </Warning>
     *
     * <Note>
     * Error `message` values may be returned as translation keys (for example `api.validation.dateTime.maxRange`) rather than finalized English strings. Treat the `code` and the field name under `errors` as the stable contract.
     * </Note>
     *
     * **Note:** The endpoint can retrieve data not older than 6 months from the current month. For older data, use the Report on the History page.
     *
     * @param {WhitebitApi.ConvertHistoryRequest} request
     * @param {WhitebitApiClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link WhitebitApi.BadRequestError}
     * @throws {@link WhitebitApi.UnprocessableEntityError}
     *
     * @example
     *     await client.convertHistory({
     *         fromTicker: "BTC"
     *     })
     */
    public convertHistory(
        request: WhitebitApi.ConvertHistoryRequest = {},
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): core.HttpResponsePromise<WhitebitApi.ConvertHistoryResponse> {
        return core.HttpResponsePromise.fromPromise(this.__convertHistory(request, requestOptions));
    }

    private async __convertHistory(
        request: WhitebitApi.ConvertHistoryRequest = {},
        requestOptions?: WhitebitApiClient.RequestOptions,
    ): Promise<core.WithRawResponse<WhitebitApi.ConvertHistoryResponse>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-TXC-PAYLOAD": requestOptions?.txcPayload ?? this._options?.txcPayload,
                "X-TXC-SIGNATURE": requestOptions?.txcSignature ?? this._options?.txcSignature,
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (
                        (await core.Supplier.get(this._options.environment)) ??
                        environments.WhitebitApiEnvironment.Default
                    ).base,
                "api/v4/convert/history",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as WhitebitApi.ConvertHistoryResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new WhitebitApi.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 422:
                    throw new WhitebitApi.UnprocessableEntityError(
                        _response.error.body as unknown,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WhitebitApiError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/v4/convert/history");
    }
}
